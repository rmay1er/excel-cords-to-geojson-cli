# 🗺️ excel-cords-to-geojson

CLI приложение для преобразования координат из Excel файлов в формат GeoJSON.

![Go](https://img.shields.io/badge/Go-1.25.1-blue)
![License](https://img.shields.io/badge/License-MIT-green)

## 📖 Описание

**excel-cords-to-geojson** — это мощный инструмент командной строки для автоматизации преобразования географических координат из Excel таблиц в стандартный формат GeoJSON.

### Основные возможности

- 📊 **Чтение Excel файлов** — поддерживает произвольные столбцы и листы
- 🗺️ **GeoJSON интеграция** — добавляет новые точки в существующий GeoJSON файл
- ⚙️ **Гибкая конфигурация** — все параметры задаются через YAML файл
- 🎨 **Кастомизация** — настройка цвета маркеров и других параметров
- ✅ **Валидация данных** — проверка корректности координат
- 📍 **Поддержка множества листов** — работает с любыми листами в Excel

## 🚀 Быстрый старт

### Установка зависимостей

```bash
go mod tidy
```

### Сборка приложения

```bash
go build -o excel-cords-to-geojson main.go
```

### Использование

1. Скопируйте пример конфигурации:
```bash
cp config.example.yaml config.yaml
```

2. Отредактируйте `config.yaml` под ваши данные

3. Запустите приложение:
```bash
./excel-cords-to-geojson convert --config config.yaml
```

## 📋 Конфигурационный файл

### Пример config.yaml

```yaml
excel:
  file: "data.xlsx"              # Путь к Excel файлу
  sheet: "Sheet1"                # Название листа (опционально)
  columns:
    name: "A"                    # Столбец с названиями точек
    description: "B"             # Столбец с описаниями
    coordinates: "C"             # Столбец с координатами
  start_row: 2                   # Начало данных (опционально)

geojson:
  input: "base.geojson"          # Базовый GeoJSON файл
  output: "result.geojson"       # Результирующий файл

appearance:
  marker_color: "#FF0000"        # Цвет маркера (опционально)
```

### Параметры

#### Excel

| Параметр | Описание | Тип | Обязательный | По умолчанию |
|----------|---------|-----|-------------|--------------|
| `file` | Путь к Excel файлу | string | ✅ Да | - |
| `sheet` | Название листа | string | ❌ Нет | "Sheet1" |
| `columns.name` | Буква столбца с названием | string | ✅ Да | - |
| `columns.description` | Буква столбца с описанием | string | ✅ Да | - |
| `columns.coordinates` | Буква столбца с координатами | string | ✅ Да | - |
| `start_row` | Строка начала данных | int | ❌ Нет | 2 |

#### GeoJSON

| Параметр | Описание | Тип | Обязательный |
|----------|---------|-----|-------------|
| `input` | Путь к входному GeoJSON | string | ✅ Да |
| `output` | Путь к выходному GeoJSON | string | ✅ Да |

#### Appearance

| Параметр | Описание | Тип | По умолчанию |
|----------|---------|-----|--------------|
| `marker_color` | Цвет маркера (hex) | string | "#FF0000" |

## 📊 Формат данных

### Excel файл

Таблица должна содержать следующие колонки (можно в любом порядке):

| Столбец | Содержимое | Пример |
|---------|-----------|--------|
| A | Названия точек | "Москва" |
| B | Описания | "Столица России" |
| C | Координаты | "55.75 37.62" |

**Формат координат:** `широта долгота` (через пробел) или `широта,долгота`

### GeoJSON файл

#### Входной файл (base.geojson)

Должен быть корректным GeoJSON FeatureCollection:

```json
{
  "type": "FeatureCollection",
  "features": []
}
```

#### Выходной файл (result.geojson)

Новые точки добавляются в формате:

```json
{
  "type": "Feature",
  "geometry": {
    "type": "Point",
    "coordinates": [37.62, 55.75]
  },
  "properties": {
    "iconCaption": "Москва",
    "description": "Столица России",
    "marker-color": "#FF0000"
  }
}
```

## 💻 Примеры использования

### Пример 1: Базовое использование

```bash
excel-cords-to-geojson convert -c config.yaml
```

Вывод:
```
📂 Загружаю конфигурацию из: config.yaml
✅ Конфигурация загружена успешно
  📊 Excel файл: data.xlsx (лист: Sheet1)
  📍 Столбцы: название=A, описание=B, координаты=C
  🗺️  GeoJSON: base.geojson → result.geojson

🔧 Инициализирую приложение...

🔄 Начинаю преобразование координат...

✅ Успешно! Результат сохранен в: result.geojson
```

### Пример 2: Работа с разными листами

Создайте несколько конфигурационных файлов:

**config_cities.yaml:**
```yaml
excel:
  file: "data.xlsx"
  sheet: "Cities"
  columns:
    name: "A"
    description: "B"
    coordinates: "C"
  start_row: 2

geojson:
  input: "base.geojson"
  output: "cities.geojson"

appearance:
  marker_color: "#0000FF"
```

Запуск:
```bash
excel-cords-to-geojson convert -c config_cities.yaml
```

## 🏗️ Архитектура проекта

```
excel-cords-to-geojson/
├── cmd/                              # Команды CLI
│   ├── root.go                       # Корневая команда
│   └── convert.go                    # Команда convert
├── internal/
│   ├── config/
│   │   └── config.go                 # Парсинг конфигурации (Viper)
│   └── domain/
│       ├── app/
│       │   └── app.go                # Главное приложение
│       ├── cords/
│       │   └── cords.go              # Модель координат
│       ├── wokerexcel/
│       │   └── excel_woker.go        # Работа с Excel
│       └── wokergeo/
│           └── geojson_woker.go      # Работа с GeoJSON
├── main.go                           # Точка входа
├── config.example.yaml               # Пример конфигурации
├── go.mod                            # Зависимости Go
└── README.md                         # Этот файл
```

## 🔧 Технический стек

- **Go** 1.25.1 — язык программирования
- **Cobra** — framework для CLI приложений
- **Viper** — управление конфигурацией
- **excelize** — работа с Excel файлами
- **go.geojson** — работа с GeoJSON форматом

## 📦 Зависимости

Все зависимости указаны в `go.mod` и будут загружены автоматически при первой сборке.

### Основные зависимости

```
github.com/spf13/cobra           - CLI framework
github.com/spf13/viper           - конфигурация
github.com/xuri/excelize/v2      - работа с Excel
github.com/paulmach/go.geojson   - GeoJSON формат
```

## 🐛 Решение проблем

### Ошибка: "лист 'Sheet1' не найден"

**Причина:** Неверное название листа в конфигурации.

**Решение:**
1. Откройте Excel файл
2. Проверьте точное название листа (вкладка внизу)
3. Обновите `excel.sheet` в конфигурации

### Ошибка: "неверное название колонки"

**Причина:** Указана неправильная буква столбца (не латинская буква).

**Решение:**
1. Используйте стандартные буквы A, B, C, D и т.д.
2. Проверьте, что это строчные или прописные буквы (без разницы)

### Ошибка: "не найдено координат"

**Причина:**
- Неправильный формат координат
- Неверный `start_row`
- Пустые ячейки в столбце координат

**Решение:**
1. Проверьте формат: `широта долгота` или `широта,долгота`
2. Убедитесь, что `start_row` указывает на первую строку данных
3. Заполните координаты для всех строк

## 📚 Документация

Подробная документация находится в папке `docs/`:
- `docs/USAGE.md` — полное руководство по использованию

## ✅ Тестирование

Для тестирования используйте файлы из примеров:

1. Создайте test Excel файл
2. Создайте test GeoJSON файл
3. Создайте конфигурацию
4. Запустите приложение
5. Проверьте результат

## 📝 Лицензия

MIT License - смотрите файл LICENSE для деталей.

## 👤 Автор

Ruslan Mayer

## 🙏 Спасибо

- Спасибо библиотеке Cobra за отличный CLI framework
- Спасибо Viper за простое управление конфигурацией
- Спасибо excelize за работу с Excel файлами
- Спасибо go.geojson за GeoJSON поддержку

## 📞 Поддержка

Если возникли вопросы или проблемы:

1. Проверьте раздел "Решение проблем" выше
2. Проверьте примеры в `config.example.yaml`

---

**Версия:** 1.0.0  
**Последнее обновление:** 07.12.2025
